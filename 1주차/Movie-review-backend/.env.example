# prod(도커 배포) 환경에서 쓰는 값. 실제 .env 는 깃에 올리지 않는다.
SECRET_KEY=change-me
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=mydatabase
DB_USER=root
DB_PASSWORD=1234
DB_HOST=mysqldb     # 백엔드도 도커일 때는 컨테이너 이름. 로컬 실행이면 127.0.0.1
DB_PORT=3306
